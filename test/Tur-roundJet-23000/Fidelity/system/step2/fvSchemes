/*--------------------------------*- C++ -*----------------------------------*\
| =========                 |                                                 |
| \      /  F ield         | OpenFOAM: The Open Source CFD Toolbox           |
|  \    /   O peration     | Version:  10                                  |
|   \  /    A nd           | Web:      www.OpenFOAM.org                      |
|    \/     M anipulation  |                                                 |
\*---------------------------------------------------------------------------*/
FoamFile
{
    version	   2.0;
    format	   ascii;
    class	   dictionary;
    location	  "system";
    object	   fvSchemes;
}
//    author      Huakun Huang
//    Email:      huanghuakun0902@163.com
// ************************************************************************* //
#include "paras2.H"
ddtSchemes
{
     default     steadyState;
}

gradSchemes
{
     default      Gauss linear    ;
     grad(p)  cellMDLimited edgeCellsLeastSquares 0.1; //no effect
     grad(U)  cellMDLimited edgeCellsLeastSquares 0.1;
     grad(k)  cellLimited   edgeCellsLeastSquares 1;
     grad(omega) cellLimited edgeCellsLeastSquares 1;
     grad(h)   cellLimited edgeCellsLeastSquares 1;

}

divSchemes
{
     div(phi,U)	       bounded  Gauss linearUpwind grad;
     div(phi,p)        bounded  Gauss upwind;
     div(phi,h)        bounded Gauss  linearUpwind grad(h);
     div(phi,K)        bounded  Gauss linear;
     div(phi,k)	bounded Gauss  linearUpwind grad;//linear;
     div(phi,omega)	bounded Gauss  limitedLinear 1;//linear;//upwind;//CoBlended 1 upwind 10 limitedLinear 1;
     div(((rho*nuEff)*dev2(T(grad(U)))))           Gauss linear;
     div(phi,symm(grad(U))) Gauss linear;
}

laplacianSchemes
{
	default	Gauss linear corrected;
}
interpolationSchemes
{
	default	linear;
}
snGradSchemes
{
	default	corrected;
}
fluxRequired
{
	default	no;
	p_rgh	;
}
wallDist
{
	method	meshWave;
	nRequired	true;
}
// ************************************
// ************************************************************************* //
